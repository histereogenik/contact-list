import * as Icon from 'react-bootstrap-icons'

const ContactList = () => {
  const searchIcon = <Icon.Search />
  return (
    <main>
      <textarea defaultValue="Search" />
    </main>
  )
}
export default ContactList
